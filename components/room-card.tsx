import Link from "next/link";
import { money, type Room } from "../lib/content";
export function RoomCard({ room }: { room: Room }) { return <article className="overflow-hidden rounded-2xl bg-white shadow-sm ring-1 ring-ink/10"><div className="photo-slot aspect-[4/3]" role="img" aria-label="Space reserved for a property photo"/><div className="p-6"><p className="eyebrow">Stay</p><h3 className="display mt-2 text-3xl">{room.name}</h3><p className="mt-2 text-sm leading-6 text-ink/75">{room.description}</p><p className="mt-5 font-bold">{money(room.daily)} / day <span className="font-normal text-ink/55">· {money(room.monthly)} / month</span></p><Link className="mt-5 inline-block text-sm font-bold underline underline-offset-4" href={`/stay/${room.slug}`}>View room details</Link></div></article> }
